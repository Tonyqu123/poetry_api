parameters:
  - name: productTemplate
    in: query
    description: productTemplate name
    required: true
    schema:
      type: string
get:
  security:
    - ApiKeyAuth: []
    - ApiRootKeyAuth: []
  tags:
    - AnalysisCenter
  summary: 获取产品的测试任务通过率
  responses:
    '200':
      description: 测试任务通过率
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/TestPassingRate'
